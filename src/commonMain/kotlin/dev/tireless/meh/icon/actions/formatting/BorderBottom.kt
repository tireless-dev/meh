package dev.tireless.meh.icon.actions.formatting

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.graphics.SolidColor
import androidx.compose.ui.graphics.vector.ImageVector
import androidx.compose.ui.graphics.vector.path
import androidx.compose.ui.unit.dp

val BorderBottom: ImageVector
    get() {
        val current = _borderBottom
        if (current != null) return current

        return ImageVector.Builder(
            name = "MehTheme.BorderBottom",
            defaultWidth = 32.0.dp,
            defaultHeight = 32.0.dp,
            viewportWidth = 32.0f,
            viewportHeight = 32.0f,
        ).apply {
            // <rect width="26" height="2" x="3.0" y="27.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 29 29
                moveTo(x = 29.0f, y = 29.0f)
                // l -26 0
                lineToRelative(dx = -26.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 26 -0z
                lineToRelative(dx = 26.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="27.0" y="23.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 29 25
                moveTo(x = 29.0f, y = 25.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="27.0" y="19.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 29 21
                moveTo(x = 29.0f, y = 21.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="27.0" y="15.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 29 17
                moveTo(x = 29.0f, y = 17.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="27.0" y="11.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 29 13
                moveTo(x = 29.0f, y = 13.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="27.0" y="7.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 29 9
                moveTo(x = 29.0f, y = 9.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="27.0" y="3.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 29 5
                moveTo(x = 29.0f, y = 5.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="23.0" y="3.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 25 5
                moveTo(x = 25.0f, y = 5.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="19.0" y="3.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 21 5
                moveTo(x = 21.0f, y = 5.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="15.0" y="3.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 17 5
                moveTo(x = 17.0f, y = 5.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="7.0" y="3.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 9 5
                moveTo(x = 9.0f, y = 5.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="11.0" y="3.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 13 5
                moveTo(x = 13.0f, y = 5.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="3.0" y="23.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 5 25
                moveTo(x = 5.0f, y = 25.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="3.0" y="19.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 5 21
                moveTo(x = 5.0f, y = 21.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="3.0" y="15.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 5 17
                moveTo(x = 5.0f, y = 17.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="3.0" y="11.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 5 13
                moveTo(x = 5.0f, y = 13.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="3.0" y="7.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 5 9
                moveTo(x = 5.0f, y = 9.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="2" height="2" x="3.0" y="3.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 5 5
                moveTo(x = 5.0f, y = 5.0f)
                // l -2 0
                lineToRelative(dx = -2.0f, dy = 0.0f)
                // l 0 -2
                lineToRelative(dx = 0.0f, dy = -2.0f)
                // l 2 -0z
                lineToRelative(dx = 2.0f, dy = -0.0f)
                close()
            }
            // <rect width="10" height="2" x="8.0" y="10.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 8 10
                moveTo(x = 8.0f, y = 10.0f)
                // h 10
                horizontalLineToRelative(dx = 10.0f)
                // v 2
                verticalLineToRelative(dy = 2.0f)
                // h -10z
                horizontalLineToRelative(dx = -10.0f)
                close()
            }
            // <rect width="6" height="2" x="8.0" y="15.0" fill="#000" />
            path(
                fill = SolidColor(Color(0xFF000000)),
            ) {
                // M 8 15
                moveTo(x = 8.0f, y = 15.0f)
                // h 6
                horizontalLineToRelative(dx = 6.0f)
                // v 2
                verticalLineToRelative(dy = 2.0f)
                // h -6z
                horizontalLineToRelative(dx = -6.0f)
                close()
            }
            // <rect width="32" height="32" fill="#000" style="fill:none" />
            path(
                fill = SolidColor(Color.Transparent),
            ) {
                // M 0 0
                moveTo(x = 0.0f, y = 0.0f)
                // h 32
                horizontalLineToRelative(dx = 32.0f)
                // v 32
                verticalLineToRelative(dy = 32.0f)
                // h -32z
                horizontalLineToRelative(dx = -32.0f)
                close()
            }
        }.build().also { _borderBottom = it }
    }

@Suppress("ObjectPropertyName")
private var _borderBottom: ImageVector? = null
